# InfluenceOpt

InfluenceOpt is a desktop app to select influencer seeds under budget and risk constraints.

Quick start:

1. python -m venv venv
2. pip install -r requirements.txt
3. python main.py

Use "Load Demo Dataset" to try the sample dataset.
